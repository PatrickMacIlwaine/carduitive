apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: carduitive
spec:
  selector:
    app: backend
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30080
  type: NodePort
