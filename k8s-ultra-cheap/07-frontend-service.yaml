apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: carduitive3
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30081
  type: NodePort
